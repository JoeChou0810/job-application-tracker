# 後端環境變數範本
# 服務埠號 (Render 會自動注入，本機開發預設 5000)
PORT=5000

# 執行環境模式
NODE_ENV=production

# 允許跨域請求的 React 前端網址 (例如您的 Vercel 專案網址)
FRONTEND_URL=https://job-application-tracker.vercel.app

# SQLite 資料庫檔案儲存路徑 (選填，在 Render 上若有設定 Persistent Disk 請指向該掛載路徑，例如 /var/data/database.sqlite)
DATABASE_PATH=
